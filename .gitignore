# --- secrets: never commit ---
.env
.env.*
*.key
*.pem
secrets/

# --- internal strategy / planning: NEVER publish (competitor analysis, pitch) ---
TRACK_DECISION.md
KNOW_RAZORPAY.md
PROJECT_SPEC.md
_internal/

# --- generated data: reproducible from seed (see README); keep only the manifest ---
data/*.json
data/*.csv
!data/manifest.json

# --- python / os ---
__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# runtime output (regenerated)
out/

# out-of-sample validation scratch
.oos/
data_large/
out_large/
data/sealed/
!.env.example

# webapp multi-month demo data (generated on first run)
sample_data/

*.egg-info/
.coverage
coverage.json
htmlcov/
mutants/

# stray research/scratch files (not part of untangle)
razorpay_public_repos.tsv
scripts/worker.py

# landing-page dev toolchain (Node build inputs/outputs — not needed at runtime;
# the app serves the committed webapp/static/landing.css + webapp/templates/landing.html)
node_modules/
.npm/
tools/tailwind/tailwind.out.css
# design handoff export — build input only; the built landing.html is the committed artifact
tools/tailwind/stitch_source.html
# local test virtualenv
.venv-test/

# setuptools/PEP517 build outputs (generated by `python -m build` / pip wheel)
build/
dist/
